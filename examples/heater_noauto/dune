(rule
 (targets heater.ml heater_main.ml)
 (deps (:zl heater.zls))
 (action (run zeluc -gtk2 -s main -o heater_main %{zl})))

(executable
  (name heater_main)
  (libraries lablgtk2 zelus zelus-gtk)
  (promote (until-clean)))
