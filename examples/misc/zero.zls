(* compare a zero-crossing of a continuous function *)
(* w.r.t a discontinuous one *)
open Unsafe

let hybrid main () =
  let der t = 1.0 init 0.0 in
  present (up((t -. 1.0))) -> (print_string "1"; print_newline ()) else ()


 
