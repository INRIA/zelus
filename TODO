for loops.

Change the data type.

- the tag "resume/reset" must be attached to the forward construct
only. currently, the syntax allow forward resume ... (and not
foreach resume which would be useless).

Add a reset without condition.

reset eq done

reset e done

resets at every step.

...
| Ereset of expression * expression option

...
| EQreset of equation * expression option
