let f x = let y = x + 1 in y + 2

let g x = inline f (x + x + 1)

let h () = g 0

let node f422(last a) returns (o)
    o = 42 -> (last a)

(* let node first() returns (o)
  emit o = 1

let node f() returns (o1)
    o1 = forward i in 0 to 4, xi in [|1;2;3;4;5|] returns (o init 42) do
           o = last o + xi + i
         done *)
