let const f<<n>>(f)(x) returns (y)
  forward(n) (xi in x) returns (yi out y)
    do yi = f xi done
    
(* let map<<n>>(f)(x:[n]'a) returns (y:[n]'b)
 forward[i] (xi in x) returns (yi out y)
    do yi = f(xi) + i done *)

(* let node f0 g x = run g x

let const fix (const f) x = let rec y = f x y in y

let fix2 f x = let rec y = f x y in y

let g x = fix (fun x y -> x) x

let pid (p, i, d)(integr)(deriv) =
  node (e) returns (u)
    u = run integr (i *. e) +. run deriv(d *. e) +. p *. e

let h = 0.1

let mon_pid(p, i, d) (e) =
 pid(p,i,d)
    (node x' -> let rec x = 0.0 fby (x *. h *. x') in x)
     (node x -> (x -. pre(x)) /. h)

let pid_ = mon_pid(0.1,0.1,0.1) *)

    

