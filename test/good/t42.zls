(* Derivative that depends on corresponding state variable through the
   intermediary of a local variable. See also: t0041. *)
let hybrid main () = x where
  rec init x = 0.0
  and der x = (let y = x in y)

