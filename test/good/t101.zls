
(*
let hybrid hubble z = y where
  rec automaton
      | S -> do
          y = 3.0
        until z then S
 *)
  
let hybrid main () = y where
  rec z = period(1.0)
  and der y = 1.0 init -1.0 reset z -> -2.0

