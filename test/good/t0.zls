let f(g)(x) = g(x + 1)

let h(x) = x + 1

let main x = f h x
    
(*
let hybrid f (x) =
  let rec der o = x +. 1.0 init 0.0 in
  let z = up(o +. x) in
  let h = 1 + 2 in
  let r = print_int h in
  let t = r in
  o, z
 *)
  
(*
let node g(x) = x + 1

let node f(g)(x, y) = run g 1 + 2

let node hh y =
  let rec o = 1 -> pre o + 1 in
       f g (42, o) + f g (42, o)
  *)  
