(*
  Block containing a hybrid function application and a period in parallel.
 *)
let hybrid hyfun rate = pos where
  rec init pos = 0.0
  and automaton
    | S -> do
          der pos = 0.0
        done

let hybrid model () =
  let leg_pos = hyfun 1.0
  and second = period (1.0) in
  false

