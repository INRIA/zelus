let hybrid iplus (x, y) =
  let rec der xi = x +. yi init 0.0 
  and der yi = y init 0.0 in
  (xi, yi)

(* "blah" *)



let s = "tim 
was 
here"

let hybrid f(x) =
  let rec der m = x init 0.0 
  and der z = m init 1.0 
  and k = z and l = up(z +. 1.0) in
  let (r1, r2) = iplus(m, z) in
  iplus(r1, r2)


let hybrid main () =
  let _ = f(7.0) in
  ()

